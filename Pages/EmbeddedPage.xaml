<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="BinaryExplorer.Pages.EmbeddedPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:core="using:BinaryExplorer.Core"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <Style x:Key="EmbColumnHeader" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="{ThemeResource TextFillColorSecondaryBrush}" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>
    </Page.Resources>

    <Grid Padding="24,16,24,24" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Style="{StaticResource TitleTextBlockStyle}" Text="Embedded Files" />
            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="6" VerticalAlignment="Bottom">
                <Button Click="ExpandAll_Click" ToolTipService.ToolTip="Expand all">
                    <FontIcon Glyph="&#xE76C;" FontSize="14" />
                </Button>
                <Button Click="CollapseAll_Click" ToolTipService.ToolTip="Collapse all">
                    <FontIcon Glyph="&#xE70D;" FontSize="14" />
                </Button>
                <Button Click="Rescan_Click">
                    <StackPanel Orientation="Horizontal" Spacing="6">
                        <FontIcon Glyph="&#xE72C;" FontSize="14" />
                        <TextBlock Text="Rescan" />
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>

        <TextBlock x:Name="HeadlineText"
                   Grid.Row="1"
                   Style="{StaticResource SubtitleTextBlockStyle}"
                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" />

        <TextBlock x:Name="SummaryText"
                   Grid.Row="2"
                   TextWrapping="Wrap"
                   FontFamily="Consolas"
                   FontSize="12"
                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" />

        <InfoBar x:Name="StatusBar" Grid.Row="3" IsOpen="False" />

        <ScrollViewer Grid.Row="4">
            <ItemsControl x:Name="GroupsControl">
                <ItemsControl.ItemTemplate>
                    <DataTemplate x:DataType="core:EmbeddedHitGroup">
                        <Expander Margin="0,0,0,8"
                                  HorizontalAlignment="Stretch"
                                  HorizontalContentAlignment="Stretch"
                                  IsExpanded="{x:Bind IsExpanded, Mode=TwoWay}">
                            <Expander.Header>
                                <StackPanel Orientation="Horizontal" Spacing="10" VerticalAlignment="Center">
                                    <TextBlock Text="{x:Bind Type}"
                                               Style="{StaticResource BodyStrongTextBlockStyle}"
                                               FontFamily="Consolas" />
                                    <Border Background="{ThemeResource ControlFillColorTertiaryBrush}"
                                            CornerRadius="10"
                                            Padding="10,2,10,2"
                                            VerticalAlignment="Center">
                                        <TextBlock Text="{x:Bind Count}" FontSize="11" />
                                    </Border>
                                </StackPanel>
                            </Expander.Header>

                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="*" />
                                </Grid.RowDefinitions>

                                <Grid ColumnSpacing="12" Padding="0,4,0,4"
                                      BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                      BorderThickness="0,0,0,1">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="140" />
                                        <ColumnDefinition Width="100" />
                                        <ColumnDefinition Width="90" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="170" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Style="{StaticResource EmbColumnHeader}" Text="Offset" />
                                    <TextBlock Grid.Column="1" Style="{StaticResource EmbColumnHeader}" Text="Size" />
                                    <TextBlock Grid.Column="2" Style="{StaticResource EmbColumnHeader}" Text="Section" />
                                    <TextBlock Grid.Column="3" Style="{StaticResource EmbColumnHeader}" Text="Notes" />
                                    <TextBlock Grid.Column="4" Style="{StaticResource EmbColumnHeader}" Text="Actions" />
                                </Grid>

                                <ItemsControl Grid.Row="1" ItemsSource="{x:Bind Hits}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate x:DataType="core:EmbeddedHit">
                                            <Grid ColumnSpacing="12" Padding="0,8,0,8"
                                                  BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                                  BorderThickness="0,0,0,1">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="140" />
                                                    <ColumnDefinition Width="100" />
                                                    <ColumnDefinition Width="90" />
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="170" />
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Text="{x:Bind OffsetHex}"
                                                           FontFamily="Consolas"
                                                           IsTextSelectionEnabled="True"
                                                           VerticalAlignment="Center" />
                                                <TextBlock Grid.Column="1"
                                                           Text="{x:Bind SizeDisplay}"
                                                           VerticalAlignment="Center" />
                                                <TextBlock Grid.Column="2"
                                                           Text="{x:Bind Section}"
                                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                                           VerticalAlignment="Center" />
                                                <TextBlock Grid.Column="3"
                                                           Text="{x:Bind Notes}"
                                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                                           TextWrapping="Wrap"
                                                           VerticalAlignment="Center" />
                                                <StackPanel Grid.Column="4" Orientation="Horizontal" Spacing="4" VerticalAlignment="Center">
                                                    <Button Content="Extract" Tag="{x:Bind}" Click="ExtractButton_Click" />
                                                    <Button Content="Open" Tag="{x:Bind}" Click="OpenButton_Click" />
                                                </StackPanel>
                                            </Grid>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </Grid>
                        </Expander>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Page>
