<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="BinaryExplorer.Pages.YaraPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:services="using:BinaryExplorer.Services"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="24,16,24,24" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Style="{StaticResource TitleTextBlockStyle}" Text="YARA" />
        <TextBlock Grid.Row="1"
                   Style="{StaticResource SubtitleTextBlockStyle}"
                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                   TextWrapping="Wrap"
                   Text="Scan the loaded binary with YARA rules. Requires yara.exe on PATH or specified below. Rules can be a .yar/.yara file or a directory (scanned recursively)." />

        <Grid Grid.Row="2" ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock VerticalAlignment="Center" Text="yara.exe:" />
            <TextBox Grid.Column="1" x:Name="YaraPathBox" FontFamily="Consolas" PlaceholderText="yara.exe (on PATH) or full path" />
            <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="4">
                <Button Content="Browse..." Click="BrowseYara_Click" />
                <Button Click="Download_Click" ToolTipService.ToolTip="Download the latest YARA win64 build from GitHub releases">
                    <StackPanel Orientation="Horizontal" Spacing="6">
                        <FontIcon Glyph="&#xE896;" FontSize="14" />
                        <TextBlock Text="Download" />
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>

        <Grid Grid.Row="3" ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock VerticalAlignment="Center" Text="Rules:" Margin="0,0,8,0" />
            <TextBox Grid.Column="1" x:Name="RulesPathBox" FontFamily="Consolas" PlaceholderText="Path to .yar file or rules directory" />
            <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="4">
                <Button Content="File..." Click="BrowseRulesFile_Click" />
                <Button Content="Folder..." Click="BrowseRulesFolder_Click" />
            </StackPanel>
        </Grid>

        <StackPanel Grid.Row="4" Orientation="Horizontal" Spacing="12">
            <Button Click="Scan_Click" Style="{StaticResource AccentButtonStyle}">
                <StackPanel Orientation="Horizontal" Spacing="6">
                    <FontIcon Glyph="&#xE721;" FontSize="14" />
                    <TextBlock Text="Scan" />
                </StackPanel>
            </Button>
            <ProgressRing x:Name="ScanRing" IsActive="False" Width="20" Height="20" />
            <TextBlock x:Name="StatusText" VerticalAlignment="Center"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
        </StackPanel>

        <InfoBar x:Name="ErrorBar" Grid.Row="5" IsOpen="False" Severity="Error" Title="YARA error" />

        <ScrollViewer Grid.Row="6">
            <ItemsControl x:Name="MatchesList">
                <ItemsControl.ItemTemplate>
                    <DataTemplate x:DataType="services:YaraMatch">
                        <Expander Margin="0,0,0,6"
                                  HorizontalAlignment="Stretch"
                                  HorizontalContentAlignment="Stretch">
                            <Expander.Header>
                                <StackPanel Orientation="Horizontal" Spacing="10" VerticalAlignment="Center">
                                    <TextBlock Text="{x:Bind Rule}"
                                               Style="{StaticResource BodyStrongTextBlockStyle}"
                                               FontFamily="Consolas" />
                                    <ItemsControl ItemsSource="{x:Bind Tags}">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <StackPanel Orientation="Horizontal" Spacing="4" />
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate x:DataType="x:String">
                                                <Border Background="{ThemeResource AccentFillColorSecondaryBrush}"
                                                        CornerRadius="8" Padding="6,1,6,1">
                                                    <TextBlock Text="{x:Bind}" FontSize="11" />
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                    <Border Background="{ThemeResource ControlFillColorTertiaryBrush}"
                                            CornerRadius="10" Padding="10,2,10,2">
                                        <TextBlock FontSize="11">
                                            <Run Text="{x:Bind Strings.Count}" /><Run Text=" strings" />
                                        </TextBlock>
                                    </Border>
                                </StackPanel>
                            </Expander.Header>
                            <ItemsControl ItemsSource="{x:Bind Strings}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate x:DataType="services:YaraStringMatch">
                                        <Grid ColumnSpacing="12" Padding="0,4,0,4">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="130" />
                                                <ColumnDefinition Width="160" />
                                                <ColumnDefinition Width="*" />
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Text="{x:Bind Offset, Converter={StaticResource RvaToHex}}"
                                                       FontFamily="Consolas"
                                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                                            <TextBlock Grid.Column="1" Text="{x:Bind Identifier}" FontFamily="Consolas" />
                                            <TextBlock Grid.Column="2" Text="{x:Bind Data}"
                                                       FontFamily="Consolas" FontSize="12"
                                                       TextWrapping="Wrap"
                                                       IsTextSelectionEnabled="True" />
                                        </Grid>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </Expander>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Page>
